[package]
name = "orrery_regolith_client"
version = "0.1.0"
edition = "2021"
description = "The Bevy 0.19 visual and keyboard skin for Orrery Regolith"
license = "MIT OR Apache-2.0"

[workspace]
resolver = "2"

[dependencies]
bevy = "0.19"
orrery_core = { path = "../../crates/orrery_core" }
orrery_games = { path = "../../crates/orrery_games" }
orrery_predict = { path = "../../crates/orrery_predict" }
orrery_protocol = { path = "../../crates/orrery_protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
# Transport pins mirror gates/p1-swarm's exactly (D14): the client is the second
# process on that harness's exterior wire and must not drift versions.
iroh = "1.0.3"
iroh-base = { version = "1.0.3", default-features = false, features = ["key"] }
bytes = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "io-util"] }

[dev-dependencies]
# Test-only. `ruleset_agreement` reproduces the ruleset's own per-tick draw
# from `orrery_core::tick_rng`, which hands back a `ChaCha8Rng`; reading a
# `u32` out of one needs `RngCore` in scope. Already in this client's lock as
# a transitive dependency of `orrery_core`.
rand_core = "0.9"
