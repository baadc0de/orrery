# Unreal build and cache output; regenerated by run.sh build / maps.
unreal/MovingInteriors/Binaries/
unreal/MovingInteriors/Intermediate/
unreal/MovingInteriors/Saved/
unreal/MovingInteriors/DerivedDataCache/
# Per-tick traces and engine logs are megabytes each and reproducible from
# spike.sh / run.sh; the summaries, the rollback reports and the transition
# screenshots beside them are committed.
results/**/*.csv
results/**/log-*.txt
