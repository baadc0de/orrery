# Rust / Cargo
target
**/*.rs.bk
Cargo.lock

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs / telemetry / test output
*.log
*.tmp
p2-kill9-*/
p2-abba-*/
p2-gateway-*/

# Device-local agent memory (never committed — lives only on this machine)
.agents/memory/

# Local single-node FoundationDB dev cluster (slice 3 test harness).
# Never committed. See .fdb-dev/README.md.
.fdb-dev/
opencode.json
